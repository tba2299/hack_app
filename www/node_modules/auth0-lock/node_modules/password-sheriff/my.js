var repl = require('repl');

var ps = require('./index');

repl.start({
  eval: function (cmd, context, filename, callback) {
    var cmd2 = cmd.slice(1, -2);
    // var s = ps('excellent').missing(cmd);
    var s = ps('excellent').missingAsMarkdown(cmd2);
    console.log(s);
    callback(null, ps('excellent').check(cmd2));
  }
});

